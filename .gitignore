# Local
__pycache__/
*.pyc
.venv/
venv/

# last30days engine (cloned at runtime by pipeline / CI)
.last30days/
.last30days-run/

# site build output (pushed to gh-pages branch by CI, not committed to main)
site/

# last30days raw engine outputs copied into reports/ for reference
reports/intrum-*

# secrets / local env
.env

# OS junk
Thumbs.db
.DS_Store
