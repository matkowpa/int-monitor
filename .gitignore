# Local
__pycache__/
*.pyc
.venv/
venv/

# last30days engine (cloned at runtime by pipeline / CI)
.last30days/
.last30days-run/

# temporary worktree for gh-pages publishing
.gh-pages-wt/

# site build output (pushed to gh-pages branch by CI, not committed to main)
site/

# secrets / local env
.env

# OS junk
Thumbs.db
.DS_Store
